[build]
  base = "frontend"
  command = "CI=false npm run build"
  publish = "build"

[build.environment]
  NODE_VERSION = "20.18.0"
  REACT_APP_API_URL = "https://backend-satin-leaf-4920.fly.dev/api"

# Redirection pour le routage SPA (Single Page Application)
# Toutes les requêtes non trouvées renvoient index.html (React Router)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Redirection pour les fichiers uploadés vers le backend
# Toute requête vers /uploads/* est proxifiée vers le backend Fly.io
[[redirects]]
  from = "/uploads/*"
  to = "https://backend-satin-leaf-4920.fly.dev/uploads/:splat"
  status = 200